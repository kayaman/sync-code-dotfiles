# roadmap

## fixes

### priority

- ~~Fix bug: it must copy complete files and folders structure. It is only creating an empty folder called `.cursor`.~~ ✅ Fixed

### non priority

- ask user about overwriting target if exists
    - display git diff or conflicts
